apiVersion: v1
kind: Service
metadata:
  name: { { include "email-reply-api.fullname" . } }
  labels: { { - include "email-reply-api.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "email-reply-api.selectorLabels" . | nindent 4 } }
